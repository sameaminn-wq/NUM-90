import React from 'react'; import { projects } from '@/data/portfolioData'; import ProjectCard from '@/components/ProjectCard'; export default function ProjectsPage() { return ( <div className="max-w-4xl mx-auto px-6 py-12"> <h1 className="text-3xl font-bold text-white mb-4">Project Repositories</h1> <div className="grid gap-6 md:grid-cols-2 mt-8"> {projects.map(p => <ProjectCard key={p.id} project={p} />)} </div> </div> ); }